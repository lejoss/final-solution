import React, {PropTypes} from 'react'
import styles from './Common.css'

const Card = () => (
    <div className="card">

    </div>
)

Card.propTypes = {
    //myProp: PropTypes.string.isRequired
}

export default Card